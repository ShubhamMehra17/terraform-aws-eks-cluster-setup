# terraform-aws-eks-cluster-setup
Production-ready Amazon EKS cluster setup using Terraform — includes custom VPC, NAT gateways, subnets, route tables, VPC endpoints, IAM roles, OIDC, security groups, and managed node groups.
